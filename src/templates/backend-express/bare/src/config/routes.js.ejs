'use strict';

/**
 * Configuration file to be used for routes
 */

const routeApiPrefix = 'api';
const routeVersions = {
  v1: 'v1',
};
const routes = [
  
]; // Application routes

module.exports = {
  setRoutes: (app) => {
    routes.forEach((route) => {
      app.use(`/${routeApiPrefix}/${route.version}/${route.route}`, require.main.app(route.path));
    });

    return app;
  },
};
