version: '3.3'

services:
  node:
    build:
      context: .
    ports:
      - "5000:5000"
    secrets:
      - host_ssh_key
    stdin_open: true
    tty: true

secrets:
  host_ssh_key:
    file: ~/.ssh/id_rsa
