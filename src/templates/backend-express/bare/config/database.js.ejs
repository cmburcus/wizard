'use strict';

/**
 * Configuration file to be used for database
 */

const envConnection = {
  client: 'pg',
  connection: {
    host: process.env.DB_HOST,
    user: process.env.DB_USERNAME,
    password: process.env.DB_PASSWORD,
    database: process.env.DB_NAME,
  },
};

module.exports = {
  development: {
    client: 'pg',
    connection: {
      host: 'postgres',
      user: '<%= props.projectNameAlias %>',
      password: 'developmentPassword',
      database: '<%= props.projectNameAlias %>',
    },
    migrations: {
      tableName: 'migrations',
      stub: 'migration.stub',
    },
  },
  testing: {
    client: 'pg',
    connection: {
      host: 'postgres_testing',
      user: '<%= props.projectNameAlias %>_test',
      password: 'testingPassword',
      database: '<%= props.projectNameAlias %>_test',
    },
    migrations: {
      tableName: 'migrations',
    },
  },
  staging: envConnection,
  production: envConnection,
};
